using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface INonZoomable
{
    //this interface is used to mark objects that belongs to scene but remain the same size when zooming
    
    
}
